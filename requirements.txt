# No third-party dependencies — the scraper and server use only the
# Python 3 standard library. This file exists so hosts like Render can run
# their default build step (`pip install -r requirements.txt`) successfully.
